import { PlaceholderScreen } from '@/components/placeholder-screen';

/** Events — adaptive sport, peer support and clinics, ingested from partner calendars. Real. */
export default function EventsPage() {
  return (
    <PlaceholderScreen
      title="Events"
      blurb="Adaptive sport, peer support and clinics near you."
      note="Coming after Peers."
    />
  );
}
