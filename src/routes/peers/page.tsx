import { PlaceholderScreen } from '@/components/placeholder-screen';

/** Peers — the members deck. Real; built next. */
export default function PeersPage() {
  return (
    <PlaceholderScreen title="Peers" blurb="Members and mentors, closest first." note="Next up." />
  );
}
